# API 录制清单

> 操作 → 导出浏览器 HAR → 丢回给我

## 一、需求条目 CRUD（最优先）

### 1. 新建需求条目
- **操作**：进入某个需求规格 → 右键 → 新建 → 需求条目 → 填标题 → 填描述 → 保存
- **目标**：`POST /erm/add/addReq`
- **备注**：body 需完整，之前被截断

### 2. 编辑需求条目（单条）
- **操作**：双击某个条目标题改名字 → 回车保存
- **目标**：`POST /erm/update/updateReqList`
- **备注**：body 需完整，包含了所有列的数据

### 3. 编辑需求条目描述
- **操作**：双击某个条目描述列 → 进富文本编辑器 → 改内容 → 点其他地方保存
- **目标**：`POST /erm/update/updateReqList`
- **备注**：跟上面同一个接口，但 body 不同，都要录

### 4. 删除需求条目
- **操作**：右键需求条目 → 删除 → 确认
- **目标**：看是 POST/DELETE 哪个接口
- **备注**：当前项目可能没这个接口，录了看看

---

## 二、导出操作

### 5. 导出 Excel（右键）
- **操作**：右键需求规格 → 导出 → Excel → 等下载
- **目标**：`GET /erm/exportExcelReqSpecification`（看完整 URL 参数）

### 6. 导出 Word（右键）
- **操作**：右键需求规格 → 导出 → Word → 等下载
- **目标**：`GET /erm/exportWordReqSpecification`（看完整 URL 参数）

### 7. 导出 ReqIf
- **操作**：右键需求规格/文件夹 → 导出 → ReqIf → 填文件名 → 选属性 → 导出
- **目标**：`POST /erm/reqIf/post/exportReqIf`（body 需完整）

---

## 三、视图管理

### 8. 保存视图
- **操作**：在某个需求规格页面 → 视图下拉 → 保存当前视图 → 填名称+描述 → 保存
- **目标**：看接口路径和 body

### 9. 打开已保存的视图
- **操作**：视图下拉 → 点某个已保存视图
- **目标**：看接口路径和参数

### 10. 删除视图
- **操作**：视图下拉 → 点删除图标
- **目标**：看接口路径和参数

---9

## 四、权限分配

### 11. 打开权限弹框
- **操作**：右键需求规格 → 权限 → 查看当前权限
- **目标**：`GET /erm/get/getReqAccess?objectId=xxx`

### 12. 添加权限用户
- **操作**：权限弹框 → 选公司/部门 → 选用户 → 点保存
- **目标**：看 POST 接口路径和 body

### 13. 移除权限用户
- **操作**：权限弹框 → 点某个用户的删除图标 → 确认
- **目标**：看接口路径和参数

---

## 五、版本 & 状态

### 14. 查看版本列表
- **操作**：进入需求规格详情 → 点版本号下拉
- **目标**：`GET /erm/search/getReqSpeVersionList?objectId=xxx`

### 15. 升版
- **操作**：悬浮已发布状态 → 点击"升版"
- **目标**：看 POST 接口路径和 body

### 16. 切换状态（工作中/冻结/已发布/审批中）
- **操作**：点状态下拉 → 选一个状态
- **目标**：看接口路径和 body

---

## 六、解锁

### 17. 关闭编辑窗口（触发解锁）
- **操作**：编辑某个需求规格后 → 关闭页面或点别的 → 观察 unlock 请求
- **目标**：`POST /erm/unlockModeForCloseWindow`（body 已经有了，确认下响应格式）

---

## 七、ReDim 导入

### 18. 下载导入模板（Excel）
- **操作**：右键文件夹 → 导入 → Excel → 点"下载模板"
- **目标**：`GET /erm/downloadReqImportTemplate?templateType=excel`

### 19. 下载导入模板（Word）
- **操作**：右键文件夹 → 导入 → Word → 点"下载模板"
- **目标**：`GET /erm/downloadReqImportTemplate?templateType=word`

### 20. 导入 Excel
- **操作**：右键文件夹 → 导入 → Excel → 选文件 → 选工作表 → 配属性 → 导入
- **目标**：`POST /erm/import/importReqSpecification`（body 需完整）

---

## 如何导出 HAR

```
F12 → Network 标签 → 操作前先清空 🚫 → 做操作 → 操作完成后
→ 右键任意请求 → "Save all as HAR with content"
→ 保存为 har 文件
```

**录完一条就导出一次，文件名用编号（如 01.har、02.har），别全攒一起。**
