# 仅UI 操作清单

- **.**  — 仅ui 没有接口
- **14.** 需求规格检索 — 仅ui
- **16.** 属性 名称必填和唯一 （api修改名称 正向和反向） — 前端有校验
- **17.** 属性 前缀 （不能非字母开头，只能包含 数字和字母，长度<10，可api反向验证） — 前端有校验
- **21.** 权限搜索框 — 仅ui
- **23.** 显示大纲，显示结构，隐藏大纲 — 仅ui
- **26.** 新建时：切换发布状态，添加取值范围，重复添加取值范围，选择枚举类型，删除取值范围 — 仅ui
- **32.** 配置属性，移除属性 — 仅ui
- **49.** 分屏展示，分两块显示内容 — 仅ui

共 9 项