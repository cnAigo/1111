# ====== Playwright AI Agent 底层配置 (DeepSeek 驱动) ======

# 1. 基础 URL 必须使用兼容 OpenAI 标准的 /v1 结尾，不能用 /anthropic
OPENAI_BASE_URL="https://api.deepseek.com/v1"

# 2. 你的 DeepSeek 真实秘钥 (强烈建议测试完后去后台刷新此 Key)
OPENAI_API_KEY="sk-75c9a07f1f444c43b332ca3d2fe5004d"

# 3. 指定 Playwright Agent 默认使用的模型
# DeepSeek 目前官方支持的最佳文本/代码模型是 deepseek-chat (即 V3)
PLAYWRIGHT_AGENT_MODEL="deepseek-v4-pro"

# (可选) 如果你后续自己写脚本直接调用 API，可以统一定义这个变量
DEFAULT_AI_MODEL="deepseek-v4-pro"