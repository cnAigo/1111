package org.example.testvue.repository;

import org.example.testvue.entity.UiProject;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UiProjectRepository extends JpaRepository<UiProject, Long> {
}
