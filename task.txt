【架构师指令：构建精准测试服务化平台 (TaaS Console) MVP版】

当前项目是一个 Spring Boot 工程，请开启完全自治模式，阅读当前项目的代码，并完成以下三个模块的开发：

任务 1：编写后端执行控制器 (TestRunnerController.java)
- 在 src/main/java/org/example/testvue/controller/ 下新建 TestRunnerController。
- 提供一个 POST 接口 /api/test/run，接收参数 module（如：文件夹、用户、全部）。
- 逻辑：根据传入的 module 动态拼接 Maven 测试命令。
- 使用 Java 的 ProcessBuilder 在后台异步执行两条命令：先执行 mvn test，测试完成后，执行 mvn allure:report 生成静态报告。
- 注意：请使用 @Async 异步执行，并立刻给前端返回 "测试已启动" 的 JSON 响应。

任务 2：配置静态资源映射 (WebConfig.java)
- 在 config 目录下新建 WebConfig.java 实现 WebMvcConfigurer。
- 覆盖 addResourceHandlers 方法，将 /allure-report/** 映射到本地的 file:target/site/allure-maven-plugin/ 目录。

任务 3：编写极简前端发射台 (index.html)
- 在 src/main/resources/static/ 目录下新建 index.html。
- 页面使用原生的 HTML+CSS+JS。
- UI 包含：顶部是模块选择按钮和“开始测试”按钮；中间是状态提示区；底部是一个 iframe 标签，src 指向 /allure-report/index.html。
- 交互逻辑：点击按钮后请求 /api/test/run 接口，倒计时结束后将底部的 iframe 显示出来展示 Allure 报告。

请自行创建代码，完成后启动 Spring Boot 服务并告诉我访问地址。



import com.microsoft.playwright.*;
import com.microsoft.playwright.options.*;
import static com.microsoft.playwright.assertions.PlaywrightAssertions.assertThat;
import java.util.*;

public class Example {
  public static void main(String[] args) {
    try (Playwright playwright = Playwright.create()) {
      Browser browser = playwright.chromium().launch(new BrowserType.LaunchOptions()
        .setHeadless(false).setSlowMo(300));
      BrowserContext context = browser.newContext(new Browser.NewContextOptions()
        .setViewportSize(1920, 1080).setIgnoreHTTPSErrors(true));
      Page page = context.newPage();
      page.navigate("https://192.168.6.171:8088/#/RequirementManagement");
      page.waitForTimeout(2000);

      // 定位需求行并打开编辑
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("新建子级")).click();
      page.getByRole(AriaRole.ROW, new Page.GetByRoleOptions().setName("req-")).click();
      page.locator(".req-content-container > div").first().dblclick();
      page.locator(".req-content-container").click(new Locator.ClickOptions()
        .setButton(MouseButton.RIGHT));
      page.getByRole(AriaRole.ROW, new Page.GetByRoleOptions().setName("req-")).click(new Locator.ClickOptions()
        .setButton(MouseButton.RIGHT));
      page.getByText("编辑正文").click();
      page.locator("#w-e-textarea-1").press("Control+a");
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("H1")).click();
      page.locator("#w-e-textarea-2").press("Control+a");
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("H2")).click();
      page.locator("#w-e-textarea-2").press("Control+a");
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("H3")).click();
      page.locator("#w-e-textarea-3").press("Control+a");
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("H4")).click();
      page.locator("#w-e-textarea-3").press("Control+a");
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("粗体\\a ctrl+b")).click();
      page.locator("#w-e-textarea-3").press("Control+a");
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("清除格式")).click();
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("清除格式")).click();
      page.locator("div").filter(new Locator.FilterOptions().setHasText(Pattern.compile("^123$"))).first().click();
      page.locator("#w-e-textarea-3").press("Control+a");
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("文字颜色")).click();
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("文字颜色")).click();
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("文字颜色")).click();
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("背景色")).click();
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("背景色")).click();
      page.locator("div").filter(new Locator.FilterOptions().setHasText(Pattern.compile("^123$"))).first().click();
      page.getByRole(AriaRole.CELL, new Page.GetByRoleOptions().setName("123")).click(new Locator.ClickOptions()
        .setButton(MouseButton.RIGHT));
      page.getByText("编辑正文").click();
      page.locator("#w-e-textarea-4").press("Control+a");
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("文字颜色")).click();
      page.locator("li:nth-child(12) > .color-block").click();
      page.locator("div").filter(new Locator.FilterOptions().setHasText(Pattern.compile("^3\\/1000123$"))).nth(1).click();
      page.getByRole(AriaRole.HEADING, new Page.GetByRoleOptions().setName("123")).click();
      page.getByRole(AriaRole.HEADING, new Page.GetByRoleOptions().setName("123")).click();
      page.getByText("123").dblclick();
      page.locator("#w-e-textarea-5").press("Control+a");
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("背景色")).click();
      page.locator("li:nth-child(21) > .color-block").click();
      page.getByRole(AriaRole.TABLE).locator("div").filter(new Locator.FilterOptions().setHasText("req-00000001默认字号H1H2H3H4")).nth(1).click();
      page.getByRole(AriaRole.TABLE).locator("div").filter(new Locator.FilterOptions().setHasText("req-00000001默认字号H1H2H3H4")).nth(1).dblclick();
      page.locator("div").filter(new Locator.FilterOptions().setHasText(Pattern.compile("^123$"))).first().click();
      // 保存 & 刷新
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("保存")).click();
      page.waitForTimeout(500);
      page.getByRole(AriaRole.BUTTON, new Page.GetByRoleOptions().setName("刷新")).click();
    }
  }
}